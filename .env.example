PORT=8080
DATABASE_URL=postgres://user:password@localhost:5432/dbname?sslmode=disable
JWT_SECRET=change-me-to-a-long-random-string

# Password reset emails (leave SMTP_HOST empty to log the reset link instead of sending an email)
APP_BASE_URL=http://localhost:8080
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
